**5. ¿Si quiero volver a modificar los permisos del fichero xattr.conf, para que el grupo de usuarios pueda escribir y ejecutar el fichero, y el propietario también pueda ejecutarlo, dejando el resto de permisos como están, tendría que usar?**

- a) `chmod a+x,g+wx /etc/xattr.conf`
    
- b) `chmod uo+wx /etc/xattr.conf`
    
- c) `chmod u-x,g+wx /etc/xattr.conf`
    
- d) **Ninguna de las anteriores es correcta** (Marcada como correcta con un check verde)
    

**6. ¿Cuál sería la salida si ejecuto el siguiente comando: `find /etc -type f | wc -l` ? (solamente se tiene en cuenta lo que sale en la imagen de la página anterior)**

- a) 12
    
- b) 10
    
- c) **2** (Marcada como correcta con flecha y check verde)
    
- d) 0
    

---

### **Completa**

Considere la siguiente estructura de directorios. ¿Cuál sería la estructura resultante si se ejecutan los siguientes comandos? **(1 punto)**

**Estructura inicial:**

Plaintext

```
/home/user
├── dirX
│   ├── fileA.txt
│   └── fileB.txt
└── dirY
    ├── fileC.txt
    └── dirZ
        └── fileD.txt
```

**Comandos a ejecutar:**

1. `cp dirX/fileA.txt dirY/`
    
2. `mv dirX/fileB.txt dirY/dirZ/`
    
3. `rm dirY/fileC.txt`
    
4. `cd dirY`
    
5. `cp ../dirX/fileA.txt dirZ/`
    

---

### **Analiza**

Tenemos los siguientes ficheros (el fichero **gshadow** almacena las contraseñas con la misma lógica que el **/etc/shadow**):

**#Contenido de /etc/gshadow**

Plaintext

```
sudo:!::admin,user1,user2
developers:!::user3,user4
staff:!::user1,user5
```

**#Contenido de /etc/group**

Plaintext

```
sudo:x:27:admin,user1,user2
developers:x:1000:user3,user4
staff:x:50:user1,user5
```

**1. ¿Para qué grupo aún no se ha definido ninguna contraseña en gpasswd?**

- a) Todos la tienen definida, aunque está encriptada
    
- b) **Ninguno**  (correcto)
    
- c) El grupo sudo, al ser el de superUsuarios
    
- d) Todos tienen la contraseña vacía